#
# this is the configuration for DEV.
#
# this is a Fins Application configuration file.
#
#[model]
#class=model
#debug=1
#datasource=mysql://user:pass@host/db
#datasource=sqlite://${appdir}/config/${appname}_${config}.sqlite3

[controller]
class=controller
reload=1

[view]
class=view
reload=1

[application]
name=imgapi
class=application

# datadir is the location scanned for image files.
# place files like so: 
# image1.json image1.zfs.gz
[app]
datadir=/tmp
